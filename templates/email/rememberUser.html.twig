{% extends 'email/base.html.twig' %}
{% block header %}
    -- {{ 'Erinnerung zu einem Event'|trans }} --
{% endblock %}

{% block content %}
    <h4 class="text-center">
        {{ room.name }}
    </h4>
    <b>
        {{'Hallo!'|trans}}
    </b>
    <p>
        {% trans with {'{name}' : room.name} %}Das Event: {name} startet gleich. {% endtrans %}
    </p>
    {% include 'email/__roomInfo.html.twig' %}

{% endblock %}
